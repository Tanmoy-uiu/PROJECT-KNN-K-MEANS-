# PROJECT-KNN-K-MEANS-
Implementation of K-Nearest Neighbors (KNN) and K-Means Clustering from scratch using Python. The project evaluates classification accuracy for different K values and analyzes clustering performance through inertia and visualization.
